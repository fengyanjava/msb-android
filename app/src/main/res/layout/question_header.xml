<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android" >

    <ImageView
        android:id="@+id/category_icon"
        android:layout_width="@dimen/question_item_category_icon_size"
        android:layout_height="@dimen/question_item_category_icon_size"
        android:contentDescription="@null"
        android:scaleType="fitXY"
        android:src="@drawable/icon_question_category_android" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="10dp"
        android:orientation="vertical" >

        <TextView
            android:id="@+id/category_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/question_item_category_text"
            android:textSize="@dimen/question_item_category_text_size" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal" >

            <TextView
                android:id="@+id/dateline"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/question_item_time"
                android:textSize="@dimen/question_item_time_size" />

            <TextView
                android:id="@+id/from"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginLeft="15dp"
                android:textColor="@color/question_item_time"
                android:textSize="@dimen/question_item_time_size" />
        </LinearLayout>
    </LinearLayout>

</merge>